services:
  bot:
    build: .
    container_name: realtyplus_bot
    restart: unless-stopped
    volumes:
      # Mount the .env file from the host to the container
      - ./.env:/app/.env
      # Optional: Mount data.json if you want to update it without rebuilding (requires restart)
      - ./data.json:/app/data.json
